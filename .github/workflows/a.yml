name: A
on:
  workflow_dispatch:
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
 a:
   name: a
   runs-on: ubuntu-latest
   timeout-minutes: 5
   steps:
     - name: Date
       run: echo "now='date'" >> $GITHUB_ENV
     - name: Echo
       run: echo "${{ github.ref }} ${{ github.ref_name }} ${{ env.now }}"
